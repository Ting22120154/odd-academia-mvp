# Secret for signing admin JWT tokens (use a long random string in production)
JWT_SECRET=your-jwt-secret-here

# Neon PostgreSQL connection string (same DB as frontend)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
