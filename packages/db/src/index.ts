// Browser-safe entry point — exports mock data only.
// Prisma client is server-only: import it directly from @prisma/client in
// API routes and server components, NOT from this package.
export * from "./mock"
